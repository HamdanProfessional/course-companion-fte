# FastAPI and ASGI Server
fastapi>=0.115.12
uvicorn[standard]==0.24.0
gunicorn==21.2.0

# Database
sqlalchemy==2.0.23
asyncpg==0.29.0
alembic==1.13.0

# Data Validation
pydantic>=2.7.0
pydantic-settings==2.1.0
email-validator==2.1.0

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0
python-multipart==0.0.6

# Rate Limiting
slowapi==0.1.9

# Cloudflare R2 Storage (S3-compatible)
boto3==1.29.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx>=0.28.1

# CORS
python-dotenv==1.0.0

# MCP Server for ChatGPT Apps (Note: local uses manual JSON-RPC 2.0, not FastMCP)
# fastmcp is NOT needed for local version

# Phase 2: LLM Support (Optional - only needed if ENABLE_PHASE_2_LLM=true)
# Uncomment the provider(s) you plan to use
openai>=1.3.0  # OpenAI API client (for GPT-4, GPT-4o-mini)
anthropic>=0.18.0  # Anthropic API client (for Claude 3)
